# 🚗 CarApp – OOP Lab (C#)

## 📌 Опис проєкту
Консольний застосунок на C#, який моделює роботу з класом **Car**.  
Програма демонструє:
- Використання **статичних полів та властивостей** (`CarCount`, `Category`).
- Реалізацію **конструкторів** (без параметрів, з двома параметрами, повний).
- Методи **Parse / TryParse** для створення об’єкта з рядка.
- Перевантаження методів (`StartEngine`, `StopEngine`).
- Статичні методи для роботи зі списком об’єктів (`AveragePrice`, `FindMostExpensive`).
- Меню для взаємодії з користувачем.

---

## 📂 Структура проєкту
CarApp/
│
├── CarType.cs # Перелік типів автомобілів (Sedan, SUV, Hatchback, Coupe, Pickup, Van)
├── Car.cs # Клас Car з конструкторами, властивостями, методами Parse/TryParse, ToString
├── Program.cs # Головна програма з меню для роботи зі списком авто 
└── README.md # Опис проєкту


---

## 🧩 Клас Car
### Основні можливості:
- **Статичні поля та властивості**
  - `CarCount` – кількість створених об’єктів.
  - `Category` – категорія автомобілів (за замовчуванням "Легкові автомобілі").
- **Властивості**
  - `Brand`, `Model`, `ManufactureDate`, `Price`, `Type`, `Color`, `VIN`, `Age`.
- **Конструктори**
  - Без параметрів (дефолтні значення).
  - З двома параметрами (`brand`, `model`).
  - Повний конструктор (`brand`, `model`, `manufactureDate`, `price`, `type`).
- **Методи**
  - `ShowInfo()` – вивід інформації про авто.
  - `StartEngine()` / `StartEngine(string mode)` – запуск двигуна.
  - `StopEngine()` / `StopEngine(bool withDelay)` – зупинка двигуна.
  - `Parse(string)` – створення об’єкта з рядка.
  - `TryParse(string, out Car)` – безпечне створення об’єкта з рядка.
  - `ToString()` – перетворення об’єкта у рядок.
  - `AveragePrice(List<Car>)` – середня ціна авто у списку.
  - `FindMostExpensive(List<Car>)` – пошук найдорожчого авто.

---

## 📜 Меню програми
При запуску користувач бачить меню:
1 – Додати об’єкт 
2 – Переглянути всі об’єкти
3 – Знайти об’єкт
4 – Продемонструвати поведінку (перевантажені методи) 
5 – Видалити об’єкт
6 – Продемонструвати static-методи 
0 – Вийти

### Опис пунктів:
- **1** – додавання авто (через конструктори або рядок з TryParse).  
- **2** – перегляд усіх авто + показ лічильника `CarCount`.  
- **3** – пошук авто за VIN або маркою.  
- **4** – демонстрація перевантажених методів (`StartEngine`, `StopEngine`).  
- **5** – видалення авто зі списку.  
- **6** – демонстрація статичних методів (`AveragePrice`, `FindMostExpensive`).  
- **0** – вихід з програми.  

---

## 🧪 Тестування
Програма протестована на:
- ✅ Коректних даних (правильні рядки для `Parse/TryParse`, валідні значення властивостей).  
- ❌ Некоректних даних (невірний формат дати, ціни, типу авто, неправильний рядок для `Parse`).  

При некоректному вводі користувач отримує інформативні повідомлення про помилки.


## 🧪 Модульні тести (CarApp.Tests)
У проєкті `CarApp.Tests` реалізовані тести MSTest для класу `Car`:
- Перевірка конструкторів.  
- Перевірка методів `Parse` та `TryParse` (коректні та некоректні дані).  
- Перевірка властивостей (`Brand`, `Price`, `ManufactureDate`).  
- Перевірка статичних методів (`AveragePrice`, `FindMostExpensive`).  
- Використання атрибутів `[TestInitialize]`, `[TestCleanup]`, `[DataRow]`, `[ExpectedException]`.  
- Використання методів `Assert.AreEqual`, `Assert.IsTrue`, `Assert.ThrowsException`.

Запуск тестів здійснюється через **Обозреватель тестов (Test Explorer)** у Visual Studio.
